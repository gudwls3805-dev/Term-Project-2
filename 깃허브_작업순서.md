# 깃허브 작업순서 (팀 공용)

Term-Project-2 협업용 Git 사용법. 헷갈리면 맨 아래 **붙여넣기용 요약**만 봐도 된다.

모든 명령어는 **Git Bash**에서 친다. 붙여넣기는 `Ctrl+V`가 아니라 **마우스 우클릭 → Paste** 또는 `Shift+Insert`.

---

## 담당과 브랜치 이름

| 담당 | 파트 | 브랜치 이름 |
|---|---|---|
| A | 데이터/DB | `feat/storage` |
| B | AI 분석 | `feat/analyzer` |
| C | 통계/대시보드 | `feat/reporter` |
| D | CLI/통합 | `feat/cli` |

브랜치 이름은 겹치면 안 된다. 본인 것만 쓴다.

---

## 0단계 — 딱 한 번만 (설치와 복사)

### 0-1. Git 설치

[git-scm.com](https://git-scm.com) → Download → 계속 Next → 완료.

**확인:** 시작 메뉴에 **Git Bash**가 생겼으면 성공.

### 0-2. 내 이름 등록 (평생 1회)

```bash
git config --global user.name "홍길동"
git config --global user.email "GitHub에 가입한 메일주소"
```

**확인:** 아무 반응 없이 다음 줄이 뜨면 성공. Git은 성공하면 조용하다.

### 0-3. 레포 복사해 오기

1. 작업할 폴더를 만든다. **폴더 이름은 `Term-Project-2`가 아닌 다른 이름으로** (예: `바탕화면\코디세이`)
2. 그 폴더에서 우클릭 → **Open Git Bash here**
3. 아래를 붙여넣고 Enter:

```bash
git clone https://github.com/gudwls3805-dev/Term-Project-2.git
cd Term-Project-2
```

4. 로그인 창이 뜨면 비밀번호 칸에 **Personal Access Token**을 붙여넣는다 (GitHub 비밀번호가 아니다)

**확인:** `ls`를 쳤을 때 파일이 보이고, 명령어 앞에 `(main)`이 떠 있으면 성공.

> ⚠️ **감쌀 폴더 이름을 `Term-Project-2`로 지으면 안 되는 이유**
> `git clone`은 자기 이름의 폴더를 알아서 하나 더 만든다. 그래서
> `Term-Project-2\Term-Project-2`처럼 두 겹이 되고, 바깥 폴더에서 git 명령을 치면
> `fatal: not a git repository`가 난다.
> 이미 그렇게 됐다면 안쪽 내용물(`.git` 포함)을 바깥으로 옮기고 빈 폴더를 지우면 된다.

---

## 1단계 — 매일 작업 시작할 때 (30초, 예외 없이)

```bash
git checkout main
git pull
```

뜻: "원본으로 돌아가서 → 어젯밤 남들이 올린 걸 받아온다"

**확인:** `Already up to date.` 또는 파일 목록이 주르륵 뜨면 성공.

이 두 줄이 사고의 90%를 막는다. 건너뛰고 작업하면 남이 이미 고친 걸 모르고 다시 고쳐서 충돌이 난다.

---

## 2단계 — 내 작업방 만들기

**처음 하는 날 (딱 1회)** — `-b`는 "새로 만들면서 이동"

```bash
git checkout -b feat/내파트
```

**그 다음부터는 매일** — 만들지 말고 이동만

```bash
git checkout feat/내파트
```

**확인:** 명령어 앞이 `(main)`에서 `(feat/내파트)`로 바뀌면 성공.
안 바뀌었다면 **지금 원본에서 작업하는 중**이니 멈춰라.

---

## 3단계 — 코드 작성하고 저장하기

메모장이든 VS Code든 편한 걸로 파일을 만들고 고친다. 이건 Git과 무관하다. 다 하고 나서:

```bash
git add .
git commit -m "리뷰를 SQLite에 저장하는 함수 추가"
```

| 명령어 | 뜻 | 비유 |
|---|---|---|
| `git add .` | 바꾼 파일 전부를 상자에 담기 | 장바구니에 넣기 |
| `git commit -m "..."` | 상자에 이름표 붙여 저장 | 결제하기 |

**확인:** `3 files changed, 45 insertions(+)`처럼 숫자가 뜨면 성공.

### 커밋 메시지 쓰는 법

"무엇을 왜 바꿨는지"를 한 줄로.

| ❌ 나쁨 | ⭕ 좋음 |
|---|---|
| `수정` | `중복 리뷰를 skip 처리하도록 변경` |
| `ㅁㄴㅇㄹ` | `별점 1~5 범위 검증 추가` |
| `최종본_진짜최종` | `한글 폰트 깨짐 수정 (Malgun Gothic 지정)` |

**빈도:** 하루에 한 번이 아니라 **기능 하나 될 때마다.** 30분~1시간에 한 번이 적당하다.

---

## 4단계 — GitHub에 올리기

**처음 올리는 날 (딱 1회)**

```bash
git push -u origin feat/내파트
```

**그 다음부터는**

```bash
git push
```

`-u`는 "이 브랜치는 앞으로 여기로 보내라"고 한 번 알려주는 것. 그래서 다음부터는 생략된다.

**확인:** GitHub 레포 페이지를 새로고침하면 노란 띠에
`feat/내파트 had recent pushes`와 **Compare & pull request** 버튼이 뜬다.

---

## 5단계 — 합쳐달라고 요청하기 (PR)

**PR(Pull Request)** = "제 작업을 원본에 넣어도 될까요?" 하는 요청서.

1. 노란 띠의 초록색 **Compare & pull request** 클릭
2. **제목:** 뭘 했는지 한 줄 — `저장소(SQLite) 모듈 구현`
3. **본문:** 왜 그렇게 했는지 2~3줄. 이게 나중에 인터뷰 답변 재료가 된다.

```text
- SQLite로 결정: 조회·필터·중복 처리를 직접 짜지 않아도 됨
- reviews 테이블 1개, 컬럼은 Phase 0 스키마 그대로
- 테스트: 샘플 30건 import 후 list 조회 확인
```

4. 오른쪽 **Reviewers** → 검토 짝 지정
5. **Create pull request** 클릭 → 여기서 내 할 일은 끝. 기다린다.

### 검토자가 하는 일

1. 알림/메일로 온 PR 링크 클릭
2. **Files changed** 탭 → 초록(추가)·빨강(삭제)으로 뭐가 바뀌었는지 본다
3. 오른쪽 위 **Review changes** → **Approve** → **Submit review**
4. 초록색 **Merge pull request** → **Confirm merge**
5. **Delete branch** 버튼이 뜨면 눌러서 정리

---

## 6단계 — 합쳐진 직후, 나머지 3명이 할 일 ⚠️

누군가의 PR이 합쳐졌다는 알림을 보면, **즉시**:

```bash
git checkout main
git pull
git checkout feat/내파트
```

이걸 빠뜨리는 게 충돌의 진짜 원인이다. 남이 이미 바꾼 걸 모른 채 계속 작업하면,
나중에 합칠 때 Git이 "둘 중 뭐가 맞냐"고 묻게 된다.

---

## 하루 전체 흐름 (A 팀원 예시)

```text
아침 9시  ─ git checkout main
          ─ git pull
          ─ git checkout feat/storage

10시 30분 ─ (저장 함수 완성)
          ─ git add .
          ─ git commit -m "리뷰 저장 함수 추가"

12시      ─ (조회 함수 완성)
          ─ git add .
          ─ git commit -m "감정별 필터 조회 추가"
          ─ git push

오후 2시  ─ GitHub에서 PR 생성, B에게 검토 요청

오후 4시  ─ B가 Approve → Merge 완료
          ─ (B·C·D는 각자 git checkout main → git pull)
```

---

## 문제가 생겼을 때

| 화면에 이게 뜨면 | 뜻 | 이렇게 한다 |
|---|---|---|
| `Please commit your changes...` | 저장 안 한 게 있어서 브랜치 이동 거부 | `git add .` → `git commit -m "작업중"` 먼저 |
| `Updates were rejected` | 서버가 나보다 앞서 있음 | `git pull` 한 뒤 다시 `git push` |
| `CONFLICT (content):` | 충돌 | 아래 설명대로 |
| `fatal: not a git repository` | 엉뚱한 폴더에 있음 | `cd Term-Project-2`로 이동 |
| `Authentication failed` | 토큰이 틀렸거나 만료 | 토큰 재발급 |
| 뭘 눌러도 안 나가고 `:`만 보임 | 편집기에 갇힘 | `q` 누르고 Enter (또는 `Esc` → `:q` → Enter) |

### 충돌(conflict) 났을 때

충돌은 에러가 아니라 **질문**이다. "같은 줄을 둘이 다르게 고쳤는데, 뭐가 맞아?"

파일을 열면 이렇게 생겼다:

```text
<<<<<<< HEAD
    별점 검증은 1~5로 한다
=======
    별점 검증은 0~5로 한다
>>>>>>> feat/analyzer
```

**해결:** 살릴 내용만 남기고 `<<<<<<<`, `=======`, `>>>>>>>` 세 줄을 지운다. 그다음:

```bash
git add .
git commit -m "충돌 해결: 별점 범위 1~5로 통일"
git push
```

> 혼자 판단하지 마라. 남의 코드가 섞인 충돌이면 담당자에게 물어보고 정한다.

---

## 절대 하지 말 것 5가지

| ❌ | 왜 |
|---|---|
| `(main)` 상태에서 코드 작성 | 원본이 오염된다. 브랜치 확인 습관을 들여라 |
| `git push -f` | 남의 작업이 통째로 사라진다. 어떤 상황에서도 쓰지 마라 |
| `config.json` 커밋 | Public 레포라 API 키가 전 세계에 공개된다. `.gitignore`에 넣고 `config.example.json`만 올린다 |
| 남의 파일 수정 | 충돌의 원인. 담당자에게 말한다 |
| 하루치 몰아서 커밋 | 충돌 규모가 커지고, 뭘 왜 바꿨는지 설명이 안 된다 |

### `.gitignore` 주의

`.gitignore`는 **줄 앞의 공백을 지우지 않는다.** 들여쓰기하면 규칙이 통째로 죽는다.

```text
   *.db     ← ❌ "이름이 공백 3칸으로 시작하는 db 파일"이라는 뜻이 되어 아무것도 못 거른다
*.db        ← ⭕
```

제대로 걸러지는지 확인하는 명령:

```bash
git check-ignore -v reviews.db output/chart.png config.json
```

세 줄이 다 나오면 정상이다. 안 나오는 파일은 실수로 커밋될 수 있다.

---

## 붙여넣기용 요약

```text
■ 작업 시작
git checkout main
git pull
git checkout feat/내파트

■ 작업 저장 (기능 하나 될 때마다)
git add .
git commit -m "무엇을 왜 바꿨는지"

■ 올리기
git push            (최초 1회만: git push -u origin feat/내파트)

■ 합친 뒤 (남의 PR이 머지됐을 때)
git checkout main
git pull
git checkout feat/내파트

■ 지금 어디 있는지 모를 때
git status
```
